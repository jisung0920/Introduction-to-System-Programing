#include <stdio.h>
#define Data int

struct Node{
	Data data;
	Node* next;
}node;

void InitNode(Node* node){
	node->data = NULL;
	Node->next = NULL;
}

int FirstInput(Node* node, int data){
	if(node->next == NULL){
		node->data = data;
		return 1;
	}
	else
		return 0;
}

int InputArray(Node* node, int data){
Node 

	
